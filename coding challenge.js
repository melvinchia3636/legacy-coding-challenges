function * generateDigitsOfPi() {let q = 1n, r = 180n, t = 60n, i = 2n;while (true) {let digit = ((i * 27n - 12n) * q + r * 5n) / (t * 5n);yield Number(digit);let u = i * 3n;u = (u + 1n) * 3n * (u + 2n);r = u * 10n * (q * (i * 5n - 2n) + r - t * digit);q *= 10n * i * (i++ * 2n - 1n);t *= u;}}; let pi_generator = generateDigitsOfPi(), pi = function(){l = [];for (var i = 0; i < 1000; i++) {l.push(pi_generator.next().value)};return l.join('')}();function pilish_string(str) {var l = [], p = 0, str=str.replace(/[^\w\d]/g, ''); while (str.length > 0) {c = pi[p];l.push(str.slice(0, c));str = str.slice(c, str.length);p++;}; return l.join(' ')}